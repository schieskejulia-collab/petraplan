This test runs the pinned classic Northwind dataset through an authority-backed evidence lifecycle without changing Source Truth. The expected release count is derived from the dataset shape: only orders with exactly one Order_Detail and a non-null ShippedDate can satisfy both the existing quantity rule and the scoped status authority claim. The evidence is then revoked and all releases that depended on it must disappear.